<!DOCTYPE html>
<html>
<head>
  <title>Wingbuddies</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>
<body>
<header>
  <nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <%= link_to "Sessions", sessions_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to "Spots", spots_path, class: 'nav-link' %>
          </li>

          <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to "Friends", friends_path, class: 'nav-link' %>
            </li>
          <% end %>
          <li>
            <%= link_to "About", about_path,  class: 'nav-link' %>
          </li>
          <% if user_signed_in? %>
            <li>
              <%= link_to "Logout", logout_path, data: { turbo_method: :delete }, class: 'nav-link' %>
            </li>
          <% else %>
            <li>
              <%= link_to "Login", login_path, class: 'nav-link' %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
</header>
<main>
  <aside id="sidebar">
    <p>
      Wingbuddies ist eine interaktive Plattform für Wingfoiler. Mit dem <a href="/sessions/">Sessionplaner</a> kannst
      Du öffentliche oder nur Deinem Freundeskreis sichtbare Sessions planen. Mit der Apple Watch-App kannst Du Deine
      Sessions tracken und ins Web laden.
    </p>
    <p>
      <% if user_signed_in? %>
        <%= link_to "Plan Session", new_session_path %> |
        <%= link_to "New Spot", new_spot_path %>
      <% end %>
    </p>
  </aside>
  <p style="color: green"><%= notice %></p>
  <%= yield %>
</main>
<footer>
  <p>Wingbuddies.de by Ralf Wirdemann</p>
</footer>
</body>
</html>
