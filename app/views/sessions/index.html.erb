<div id="sessions">
  <% @sessions.each do |session| %>
    <%= render "session", session: session %>
  <% end %>
</div>

<div id="next_link">
  <%= button_to "next", pagy_url_for(@pagy, @pagy.next) %>
</div>